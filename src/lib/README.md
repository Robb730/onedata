# Lib

This directory is used for library configurations and third-party initializations. It houses configurations for services that the application depends on, such as the `supabaseClient.js` database connection.
