# Contexts

This directory stores React Contexts and their Providers. It manages global application state, such as authentication, themes, and other state that needs to be accessed by multiple components.
