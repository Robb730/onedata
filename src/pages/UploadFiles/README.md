# Upload Files

The Upload Files module handles the intake of new data into the system. It provides interfaces and validation tools (e.g., parsing Excel spreadsheets) to safely and accurately ingest records into the centralized database, minimizing data fragmentation.
