# Components

This directory contains reusable React components for the application. Components should be structured and isolated so they can be easily reused across different pages and layouts within the system.
